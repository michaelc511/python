# TROUBLESHOOT: Common Issues

## Use your own CSV
Replace SAMPLE_DATA with `pd.read_csv("your_file.csv")`. Make sure columns are clear. Convert rows to descriptive sentences.

## "No module named pandas"
pip install pandas

## Insights too simple
Add more metrics: mean, max, by month. get_insights() is a starting point.
